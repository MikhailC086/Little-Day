import React from "react";
import ReactDOM from "react-dom/client";
import LittleDayApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LittleDayApp />
  </React.StrictMode>
);
